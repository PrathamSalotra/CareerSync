// Server entry point (Placeholder for Phase 02)
import config from './config/index.js';
import app from './app.js';

// Phase 01: Verify config parsing on startup
console.log(`Server environment configured for NODE_ENV=${config.NODE_ENV}`);
